name: flutter_zenn_api_freezed
description: API Demo Project

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # state manegment
  flutter_riverpod: ^2.0.0-dev.4

  # parser
  freezed_annotation: ^1.1.0

  # API
  dio: ^4.0.4
  pretty_dio_logger: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner:
  freezed:
  json_serializable:


flutter:
  uses-material-design: true
